# Clarity components

This folder is reserved for shared UI components and page fragments used by the static frontend shell.

The main application currently renders pages from `assets/js/app.js` and uses this folder as the canonical components directory for future expansion.
